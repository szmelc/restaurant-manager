= simple_form_for @order do |f|
  = f.simple_fields_for :meals, @order.meals.build do |meals|
    = meals.collection_select(:name, Dish.all, :name, :name)
    = meals.collection_select(:quantity, 2..4, :quantity, :quantity)
  = f.submit

