%div#home.navbutton{:data => {:link => root_path}}
	= image_tag 'home.svg'
%div#avatar-div
	- if user_signed_in?
		= image_tag current_user.user_avatar, :id => 'avatar-thumb'

- if user_signed_in?
	%div.navbutton{:data => {:link => user_path(current_user)}}
		%p= link_to current_user.name, user_path(current_user)
