= content_for :title, "Arbejder | Employees"
%div#user-list
	- @users.each do |user|
		%div.user
			= image_tag user.user_avatar, :id => "avatar", :data => {:link => user_path(user)}
			%h3= user.name 


