%table
	%tr
		%th Imię
		%th Nazwisko
		%th Adres e-mail
	- @users.each do |user|
		%tr{:class => "user-row", :data => {:link => user_path(user)}}
			%td= user.first_name
			%td= user.last_name
			%td= user.email