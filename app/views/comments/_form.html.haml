%div#form-div
	= simple_form_for ([@post, @post.comments.build]) do |f|
		%div.form-inputs
			%div.avatar
				= image_tag current_user.user_avatar, :class => 'comment-avatar-thumb'
			%div.comment
				= f.text_field :content, :placeholder => "Napisz komentarz"	
				= f.submit 'Wyślij'